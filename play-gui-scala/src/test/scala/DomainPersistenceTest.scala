package no.lau.vdvil.player

import org.junit.Test


class DomainPersistenceTest {
  @Test def testStuff {

    println(MasterMix.toXML(PlayGUI.masterMix))

    //val composition = Dvl.fromXML(XML.loadFile(xml))
  }
}